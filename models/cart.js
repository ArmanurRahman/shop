class Cart {
    constructor(productId, title, quantity, totalPrice) {
        this.productId = productId;
        this.title = title;
        this.quantity = quantity;
        this.totalPrice = totalPrice;
    }
}

export default Cart;
