class Cart {
    constructor(productId, title, imageUrl, quantity, totalPrice, price) {
        this.productId = productId;
        this.title = title;
        this.imageUrl = imageUrl;
        this.quantity = quantity;
        this.totalPrice = totalPrice;
        this.price = price;
    }
}

export default Cart;
